# /progress — Progress Dashboard

Show reading status, chapter word counts, and source coverage in one report.

The skill reads notes and chapters, counts `reading`, `completed`, and `integrated` sources, compares word counts with configured targets, and highlights coverage gaps.

It is always read-only.
