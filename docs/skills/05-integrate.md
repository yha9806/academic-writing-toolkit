# /integrate — Notes Into Chapters

Use completed reading notes to propose chapter edits.

The skill scans a notes file, reads likely insertion points, presents an integration plan, and waits for user approval before editing any chapter.

Every quote needs a page reference. Every argument needs attribution. Inserted prose follows British English.
