# /map — Literature-To-Chapter Mapping

Build a matrix showing which sources support, challenge, or extend each chapter.

The skill reads `literature/reading_notes/*_NOTES.md`, extracts the `Thesis Connections` table, detects chapter files, and reports coverage gaps.

It is read-only unless the user explicitly asks to save the matrix.
