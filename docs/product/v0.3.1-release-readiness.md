# v0.3.1 Release Readiness

`v0.3.1` is a patch release for the app/plugin distribution surface after the `manuscript-reframe` skill landed on `master`.

## Release Scope

- Add `/manuscript-reframe` to the Codex plugin package.
- Keep ChatGPT App and Codex plugin package versions aligned at `0.3.1`.
- Repair the new Agent skill entry so `.agents/skills/manuscript-reframe` is a symlink.
- Remove project-specific public-content scan triggers from the new skill docs.
- Fix the workflow typo introduced in the README and skills index.
- Keep the already-published ChatGPT App update path on the existing Hugging Face Space MCP base URL.

## Required Checks

Run before tagging:

```bash
make plugin-check
make chatgpt-app-check
make test
python .claude/skills/release-governance/scripts/check_release_packet.py .
git diff --check
```

## Expected Release Notes

Academic Writing Toolkit `v0.3.1` adds the `/manuscript-reframe` workflow for turning report-like drafts into paper-form scientific arguments, repairs the packaged skill symlink, keeps ChatGPT App and Codex plugin metadata aligned, and refreshes the OpenAI/Hugging Face submission handoff notes for the current version.

## External Publication Boundary

The repository can tag and verify `v0.3.1`, and the Hugging Face Space can be updated from the repository app package. OpenAI dashboard review still requires the maintainer-controlled Apps Manage account and completed business verification before pressing Submit for Review.
